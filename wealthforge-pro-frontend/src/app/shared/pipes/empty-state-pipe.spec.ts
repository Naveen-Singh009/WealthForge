import { EmptyStatePipe } from './empty-state-pipe';

describe('EmptyStatePipe', () => {
  it('create an instance', () => {
    const pipe = new EmptyStatePipe();
    expect(pipe).toBeTruthy();
  });
});
